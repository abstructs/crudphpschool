window.onload = (function() {
    var cards = document.getElementsByClassName('contact_card');
});


// REQUIRES: id must be in the last place of the id
function deleteContact(event, id) {
    console.log(id);
}